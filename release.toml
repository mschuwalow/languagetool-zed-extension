allow-branch = ["main"]
pre-release-commit-message = "chore: release {{version}}"
tag = true
publish = false
push = true

[[pre-release-replacements]]
file = "extension.toml"
search = '^version = "[^"]+"'
replace = 'version = "{{version}}"'
exactly = 1
